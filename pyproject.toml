[project]
name = "actuarial-sandbox"
version = "0.1.0"
description = "Simplified term-life projection (mortality & lapse)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0", "pandas>=2.0"]
ui = ["streamlit>=1.28", "pandas>=2.0", "plotly>=5.18"]

[tool.pytest.ini_options]
testpaths = ["tests"]
